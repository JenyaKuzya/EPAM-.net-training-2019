Develop a generic class Queue collection that implements the basic operations for working with the queue and allows iteration by implementing an “manually” iterator (without using the yield block iterator). Test the methods of the developed class.